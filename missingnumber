class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int n=nums.size()+1;
        int sum=n*(n-1)/2;
         
        int arraysum=0;

        for(int i=0;i<nums.size();i++){
            arraysum+=nums[i];
        }
        int ans=sum-arraysum;
        return ans;
    }
};
